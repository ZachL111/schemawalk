# Review Journal

This journal records the domain cases that matter before widening the public API.

The local checks classify each case as `ship`, `watch`, or `hold`. That gives the project a small review vocabulary that matches its databases focus without claiming live deployment or external usage.

## Cases

- `baseline`: `index fit`, score 119, lane `watch`
- `stress`: `join width`, score 157, lane `ship`
- `edge`: `constraint risk`, score 201, lane `ship`
- `recovery`: `plan drift`, score 226, lane `ship`
- `stale`: `index fit`, score 173, lane `ship`

## Note

The useful failure mode here is a wrong decision on a named case, not a vague style disagreement.
